// 5. Combining Arrays
// Create two arrays of your choice.
// Use the concat method to combine the two arrays into a new array.
// Print both the original arrays and the combined array.
const friends = ['Rakib', 'Sajid', 'Jabed', 'Tareq'];
const nums = [15, 32, 54, 75];
console.log(friends, nums);
const nu = friends.concat(nums);
console.log(nu)